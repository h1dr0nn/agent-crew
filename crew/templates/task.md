# Task <ID> — <what, in one line>

<Two or three sentences: why this task exists and what it builds on.>

## Files you own
- `<path>` (new | the <function> only)

## What to write
```<language>
<the exact signatures, types and error codes; not a description of them>
```
- <each behaviour, one bullet, with its edge cases>

## Tests
<what the tests must cover: success, each error, the edge cases above>

Verify kind: `<kind>` (from .agent-crew/project.toml)

## Inlined sources
@@include <path>#L<a>-L<b>
@@grep <path> <regex>
